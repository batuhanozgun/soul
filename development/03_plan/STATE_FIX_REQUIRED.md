# STATE CONSOLIDATION DEFECT

The builder created a transition note instead of updating the authoritative `STATE.md`. That duplicates current-state semantics and violates the single-authoritative-home rule established in Phase 0 itself.

Required repair before verification:

1. update `STATE.md` to the actual verifier-ready state,
2. delete `STATE_TRANSITION_NOTE.md`,
3. delete this defect marker,
4. ensure `NEXT_SESSION.md` and WP-001 remain navigational/work-definition references rather than competing project-state stores.

This marker is temporary and must not remain in the verifier target.
